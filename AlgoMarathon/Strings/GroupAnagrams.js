/* 
Group Anagram (Array of Strings) 
*/ 


//ATTEMPT #1 (FAILED)

var groupAnagrams = function(strs) {
    let result = []; 
    for (let i = 0; i < strs.length; i++) {
        //not entirely how to work on this problem 
        // console.log(strs[i].split('').sort()); 
        // let sortedStr = strs[i].split('').sort().join(''); 
        // console.log(sortedStr); 
        // let group = []; 
        // if (!group.includes(sortedStr)) group.push(strs[i]); 
    }
    console.log(result); 
};

/*
Progress notes: 

20m - Trying to go through the problem 

15m - Reviewed through the solutions in Leetcode and generally understand the two ways to solve the problem 

Future Attempts: 
- Work on the same problem on AlgoExpert.io 
- Try out the two approaches to solve this problem 

*/ 